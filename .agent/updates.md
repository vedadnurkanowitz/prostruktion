Updated `ProjectMap` to:
- Filter displayed projects to only include "Scheduled" and "In Progress" (Active) statuses.
- Excluded "In Abnahme" and "Finished" unless they have logged complaints.
- Ensure all dropdown filters (Sub, Partner, Contractor) still apply.
